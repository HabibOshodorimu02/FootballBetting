@model IEnumerable<FootballBetting.Models.Match>
@{
    ViewData["Title"] = "Football Matches";
}

<div class="row">
    <div class="col-md-12">
        <h1 class="display-4"><i class="fas fa-futbol"></i> Football Betting Platform</h1>
        <p class="lead">Welcome to the ultimate football prediction platform! Make your predictions and compete with
            other users.</p>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <h2><i class="fas fa-calendar-alt"></i> Upcoming Matches</h2>
        @if (Model.Any())
        {
            <div class="row">
                @foreach (var match in Model)
                {
                    <div class="col-md-6 col-lg-4 mb-4">
                        <div class="card h-100 shadow-sm">
                            <div class="card-header bg-primary text-white text-center">
                                <h6 class="mb-0">
                                    <i class="fas fa-clock"></i> @match.MatchDate.ToString("MMM dd, yyyy - HH:mm")
                                </h6>
                            </div>
                            <div class="card-body text-center">
                                <div class="row align-items-center">
                                    <div class="col-4">
                                        <h6 class="mb-1">@match.HomeTeam.Name</h6>
                                        <small class="text-muted">Home</small>
                                    </div>
                                    <div class="col-4">
                                        <h4 class="text-primary">VS</h4>
                                        @if (match.IsCompleted)
                                        {
                                            <div class="mt-2">
                                                <span class="badge bg-success">
                                                    @match.HomeScore - @match.AwayScore
                                                </span>
                                            </div>
                                        }
                                    </div>
                                    <div class="col-4">
                                        <h6 class="mb-1">@match.AwayTeam.Name</h6>
                                        <small class="text-muted">Away</small>
                                    </div>
                                </div>
                            </div>
                            <div class="card-footer text-center">
                                @if (!match.IsCompleted)
                                {
                                    @if (Context.Session.GetInt32("UserId") != null)
                                    {
                                        <a asp-controller="Prediction" asp-action="Create" asp-route-matchId="@match.Id"
                                            class="btn btn-success btn-sm">
                                            <i class="fas fa-chart-line"></i> Make Prediction
                                        </a>
                                    }
                                    else
                                    {
                                        <small class="text-muted">Login to make predictions</small>
                                    }
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Match Completed</span>
                                }
                            </div>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <div class="alert alert-info">
                <i class="fas fa-info-circle"></i> No matches available at the moment.
            </div>
        }
    </div>
</div>

<!-- Points System Information -->
<div class="row mt-5">
    <div class="col-md-12">
        <h3><i class="fas fa-star"></i> Points System</h3>
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <h5>Outcome Predictions</h5>
                        <ul class="list-unstyled">
                            <li><i class="fas fa-home text-primary"></i> Home Win: <span class="badge bg-primary">2
                                    points</span></li>
                            <li><i class="fas fa-plane text-success"></i> Away Win: <span class="badge bg-success">3
                                    points</span></li>
                            <li><i class="fas fa-handshake text-warning"></i> Draw: <span class="badge bg-warning">5
                                    points</span></li>
                        </ul>
                    </div>
                    <div class="col-md-6">
                        <h5>Score Predictions</h5>
                        <ul class="list-unstyled">
                            <li><i class="fas fa-bullseye text-danger"></i> Correct Score: <span
                                    class="badge bg-danger">10 points</span></li>
                            <li><i class="fas fa-times text-muted"></i> Wrong Prediction: <span
                                    class="badge bg-secondary">0 points</span></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
